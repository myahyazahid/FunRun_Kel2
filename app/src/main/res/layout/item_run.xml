<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="20dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#FFFFFF"
    android:layout_marginBottom="12dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Left purple accent bar -->
        <View
            android:id="@+id/accentBar"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:background="@drawable/shape_accent_bar"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- Run icon bg -->
        <TextView
            android:id="@+id/tvRunIcon"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:text="🏃"
            android:textSize="22sp"
            android:gravity="center"
            android:background="@drawable/shape_icon_bg"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="12dp"/>

        <!-- Date -->
        <TextView
            android:id="@+id/txt_run_date"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="22-03-2025"
            android:textSize="12sp"
            android:textColor="#9E9EBF"
            android:fontFamily="@font/poppins_regular"
            app:layout_constraintTop_toTopOf="@id/tvRunIcon"
            app:layout_constraintStart_toEndOf="@id/tvRunIcon"
            app:layout_constraintEnd_toStartOf="@id/btnDelete"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"/>

        <!-- Distance + Duration -->
        <LinearLayout
            android:id="@+id/statsRow"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="2dp"
            app:layout_constraintTop_toBottomOf="@id/txt_run_date"
            app:layout_constraintStart_toEndOf="@id/tvRunIcon"
            app:layout_constraintEnd_toStartOf="@id/btnDelete"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp">

            <TextView
                android:id="@+id/txt_run_distance"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="5.2 km"
                android:textSize="15sp"
                android:textColor="#2D2D3A"
                android:fontFamily="@font/poppins_bold"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=" · "
                android:textSize="15sp"
                android:textColor="#BDBDCF"/>

            <TextView
                android:id="@+id/txt_run_duration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="30 mnt"
                android:textSize="15sp"
                android:textColor="#2D2D3A"
                android:fontFamily="@font/poppins_semibold"/>
        </LinearLayout>

        <!-- Delete button -->
        <ImageButton
            android:id="@+id/btnDelete"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_person"
            android:tint="#FF5252"
            android:background="@drawable/shape_btn_delete_bg"
            android:contentDescription="Hapus"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/btnEdit"
            android:layout_marginEnd="8dp"/>

        <!-- Edit button -->
        <ImageButton
            android:id="@+id/btnEdit"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_assignment"
            android:tint="#7C4DFF"
            android:background="@drawable/shape_btn_edit_bg"
            android:contentDescription="Edit"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>